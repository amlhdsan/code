# 题解翻译

一个直观的起点是：对每个坐标 $i$（$1 \le i \le n$）都进行一次 **throw query**。设 $d_i$ 表示从坐标 $i$ 开始时小球一共会跳多少次。为了方便，定义

$$
d_{n+1} = d_{n+2} = 0
$$

注意到，对于 $1 \le i \le n$，如果 $d_{i+1} \neq d_{i+2}$，那么我们可以立即确定 $a_i$：

- 如果 $d_i = d_{i+1} + 1$，则 $a_i = 1$；  
- 否则，$a_i = 2$。  

困难在于 $d_{i+1} = d_{i+2}$ 的情况。我们称这样的下标 $i$ 为 **未知的 (unknown)**。在这种情况下，我们其实可以在不发出查询的前提下推导出 $d_i$，从而把查询省下来。关键观察如下：

**观察：** 不会有两个相邻的未知下标。

**证明：** 如果 $i$ 是未知的，那么

$$
d_i = d_{i+1} + 1
$$

因此

$$
d_i \neq d_{i+1}
$$

这说明 $i-1$ 不可能是未知的。$\square$

---

利用这个事实，我们采用如下策略：

- 在得到所有 $d_i$ 之后，对于每个未知下标 $i$（从小到大处理），交换位置 $i$ 和 $i+1$ 的盒子，然后在坐标 $i+1$ 执行一次 throw query。因为 $i+1$ 不是未知的，这样就能揭示出 $a_i$。  
- 特殊情况是 $i = n$：此时我们交换 $n-1$ 和 $n$ 的盒子，然后在坐标 $n-1$ 执行 throw query 来得到 $a_n$。  

---

## 总结步骤

1. 从 $n$ 到 $1$ 依次处理：在坐标 $i$ 处执行 throw query。若 $d_{i+1} = d_{i+2}$，则跳过 $i$ 的查询并将 $i$ 标记为未知（但仍然计算 $d_i$）；否则我们可以直接确定 $a_i$ 和 $d_i$。  
2. 对于每个 $1 \le i \le n-1$：如果 $i$ 是未知的，就交换盒子 $i$ 和 $i+1$，并在坐标 $i+1$ 执行 throw query，从而确定 $a_i$。  
3. 最后，交换 $n-1$ 和 $n$，并在坐标 $n-1$ 执行 throw query，以确定 $a_n$。  

---

## 查询次数分析

- 每个未知下标需要 $2$ 次查询；  
- 每个已知下标只需 $1$ 次查询；  
- 由于不会有两个未知下标相邻，未知下标最多有  

$$
\left\lceil \frac{n}{2} \right\rceil
$$

个。  

因此，总查询次数最多为  

$$
\left\lceil \frac{3n}{2} \right\rceil
$$
